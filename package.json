{
  "name": "vue-grab-monorepo",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.24.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:playground": "pnpm --filter playground dev",
    "docs:dev": "pnpm --filter website dev",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "lint": "oxlint --vue-plugin --vitest-plugin packages/ website/",
    "lint:fix": "oxlint --fix --vue-plugin --vitest-plugin packages/ website/",
    "format": "oxfmt --write packages/ website/",
    "format:check": "oxfmt --check packages/ website/",
    "release": "changeset publish",
    "version-packages": "changeset version"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.1",
    "@changesets/cli": "^2.27.12",
    "oxfmt": "^0.43.0",
    "oxlint": "^1.58.0",
    "turbo": "^2.8.0",
    "typescript": "~5.9.3"
  }
}

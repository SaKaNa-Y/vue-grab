# Required by @changesets/changelog-github during `pnpm version-packages`.
# Create a GitHub PAT with `public_repo` scope (https://github.com/settings/tokens)
# and paste it into a local `.env` (gitignored). Not needed in CI.
GITHUB_TOKEN=
